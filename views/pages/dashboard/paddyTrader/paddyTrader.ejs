<%- include('../../../partials/head.ejs') %>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-2">
            <%- include('../../../partials/sidebar.ejs') %>
        </div>

        <div class="col-md-10">
            <div class="row">
                <div class="col-md-11">
                    <div class="card card-body my-5">
                        <div class="text-muted" style="margin: right;">
                            <a href="/dashboard/newPaddyTrader" class="btn btn-success btn-sm mb-4">Create New Paddy Trader</a>
                        </div>

                        <h4 class="mt-3 mb-4">
                            <%= title.split('|')[0] %>
                        </h4>

                        <% if(paddyTrader.length === 0){ %>
                            <div class="text-center text-muted">
                                <p>There is no Paddy Trader available rigth now</p>
                            </div>
                        <% }else{ %>
                            <% let id = 1 %>
                            <div style="overflow-x:auto;">
                                <table>
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>নাম</th>
                                            <th>ফোন</th>
                                            <th>খরচ</th>
                                            <th>জমা</th>
                                            <th>ব্যাগ খরচ</th>
                                            <th>ব্যাগ জমা</th>
                                            <th>Details</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% for (let p of paddyTrader){ %>
                                            <div class="col-sm-3" style=" width: 200px;">
                                                <tr scope="row">
                                                    <th scope="row"><%= id %></th>
                                                    <td><%= p.banglaName%></td>
                                                    <td><%= p.phone %></td>
                                                    <td class="text-danger"><%= p.debit %></td>
                                                    <td class="text-success"><%= p.credit %></td>
                                                    <td class="text-danger"><%= p.debitBag %></td>
                                                    <td class="text-success"><%= p.creditBag %></td>
                                                    <td><a href="/dashboard/singlePaddyTrader/<%= p._id %>" target="_blank" >details</a></td>
                                                    <% id++ %>
                                                </tr>
                                            </div>
                                        <% } %>
                                    </tbody>
                                </table>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../../../partials/footer.ejs') %>
</body>
</html>